
//input

MCP Gateway migration to Auth API v2 is complete. MCP now consumes POST /v2/auth and no
 longer uses POST /v1/auth.

vector //

 [bitbucket | src/auth_client.py | similarity=0.82]
 Call POST /v1/auth … AUTH_API_VERSION = "v1"
 
 [CANDIDATE NODE uid=ep-v1 kind=Endpoint]
 POST /v1/auth
 [CANDIDATE NODE uid=api-v2 kind=Api]
 Auth API v2


 ouptut //
{
  "decisions": [
    {
      "name": "complete MCP Gateway migration to Auth API v2",
      "statement": "MCP Gateway now consumes POST /v2/auth.",
      "rationale": null,
      "status": "accepted"
    }
  ],
  "apis": [{ "name": "Auth API v2", "version": "v2", "status": "active" }],
  "endpoints": [{ "name": "POST /v2/auth", "method": "POST", "path": "/v2/auth" }],
  "systems": [],
  "terms": [],
  "facts": [
    {
      "subject_name": "MCP-150",
      "subject_kind": "WorkItem",
      "relation": "MIGRATES_TO",
      "object_name": "Auth API v2",
      "object_kind": "Api",
      "object_candidate_uid": "api-v2",
      "evidence": "MCP Gateway migration to Auth API v2 is complete."
    },
    {
      "subject_name": "complete MCP Gateway migration to Auth API v2",
      "subject_kind": "Decision",
      "relation": "APPLIES_TO",
      "object_name": "MCP-150",
      "object_kind": "WorkItem",
      "evidence": "MCP now consumes POST /v2/auth and no longer uses POST /v1/auth."
    }
  ],
  "assessments": [
    {
      "action": "architecture_change",
      "topic_key": "auth-api-v2-migration",
      "should_flag": true,
      "severity": "high",
      "title": "MCP Gateway authentication dependency migrated to Auth API v2",
      "summary": "Jira claims MCP now calls POST /v2/auth.",
      "reasoning": "NEW SOURCE states v2 complete; retrieved code still shows POST /v1/auth — flag the claim, do not treat related code as new evidence.",
      "evidence": "MCP now consumes POST /v2/auth and no longer uses POST /v1/auth.",
      "related_candidate_uids": ["api-v2"],
      "confidence": 0.8
    }
  ]
}





Evidence:
Bitbucket code calls POST /v1/auth

Fact:
MCP Gateway —CALLS_ENDPOINT→ POST /v1/auth

Knowledge:
Auth v1 deprecation impacts MCP Gateway

Wisdom:
Before removing a shared API version,
verify migration of every live consumer.


Finding 1: MCP migration claim did not match code
Finding 2: Argo owner missed deprecation
Finding 3: Another project broke after API removal

Wisdom proposal:
“Migration completion must be verified from production code,
not only Jira or documentation.”

